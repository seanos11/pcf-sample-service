spring:
  application:
    name: greetclient
  cloud:
    services:
      registrationMethod: route

server:
  port: 8082

producer:
  ribbon:
    ServerListRefreshInterval: 5000
ribbon:
  IsSecure: false

  profiles: dev

security:
  basic:
    enabled: false
